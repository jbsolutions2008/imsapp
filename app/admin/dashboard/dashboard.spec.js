'use strict';

describe('myApp.admin-dashboard module', function() {

  beforeEach(module('myApp.admin-dashboard'));

  describe('admin-dashboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('admin-dashboard-controller');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});