'use strict';

describe('myApp.admin-add-edit-product module', function() {

  beforeEach(module('myApp.admin-add-edit-product'));

  describe('admin-add-edit-product controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('admin-add-edit-product-controller');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});