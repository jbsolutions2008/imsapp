'use strict';

describe('myApp.admin-product-list module', function() {

  beforeEach(module('myApp.admin-product-list'));

  describe('admin-product-list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('admin-product-list-controller');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});