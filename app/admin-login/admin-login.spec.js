'use strict';

describe('myApp.admin-login module', function() {

  beforeEach(module('myApp.admin-login'));

  describe('admin-login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('admin-login-controller');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});